--[[
	Schema-wide configuration.

	This only sets values specific to Combine Conscript Roleplay. The full
	list of config keys the Helix framework itself exposes lives in the
	framework, not here - see the Helix documentation before adding more.
]]

ix.currency.Set("cr.", "Combine credit", "Combine credits")
